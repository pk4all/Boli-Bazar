<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= auction.title %> | BoliBazar High-Impact Auction</title>
    <!-- Google Fonts: Inter & Outfit -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&family=Outfit:wght@500;600;700;800;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/main.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary: #d90429;
            --secondary: #1a1a1a;
            --accent: #ffb703;
            --success: #2ec4b6;
        }
        body { font-family: 'Inter', sans-serif; }
        .font-heading { font-family: 'Outfit', sans-serif; }
        
        /* Premium Background Gradient */
        .bg-detail-mesh {
            background-color: #f8fafc;
            background-image: radial-gradient(at 0% 0%, rgba(217, 4, 41, 0.05) 0px, transparent 50%),
                              radial-gradient(at 100% 0%, rgba(26, 26, 26, 0.02) 0px, transparent 50%);
        }

        .sticky-sidebar { position: sticky; top: 100px; }

        /* Countdown Banner Animation */
        .countdown-banner {
            background: linear-gradient(90deg, #d90429, #ef233c);
            animation: pulse-red 2s infinite;
        }
        @keyframes pulse-red {
            0% { box-shadow: 0 0 0 0 rgba(217, 4, 41, 0.4); }
            70% { box-shadow: 0 0 0 10px rgba(217, 4, 41, 0); }
            100% { box-shadow: 0 0 0 0 rgba(217, 4, 41, 0); }
        }

        /* Tabs Styling */
        .tab-btn { position: relative; padding-bottom: 12px; transition: all 0.3s; }
        .tab-btn.active { color: var(--primary); }
        .tab-btn.active::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 100%;
            height: 3px;
            background: var(--primary);
            border-radius: 99px;
        }

        /* Leaderboard Cards */
        .leader-card {
            border: 1px solid #f1f5f9;
            transition: all 0.2s;
        }
        .leader-card:hover { border-color: var(--primary); transform: translateX(5px); }

        /* Custom Quantity Input */
        .qty-input::-webkit-inner-spin-button, 
        .qty-input::-webkit-outer-spin-button { -webkit-appearance: none; margin: 0; }

        .payment-toggle {
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }
    </style>
</head>
<body class="bg-detail-mesh min-h-screen pb-24">
    <!-- Top Sticky Progress Bar (Glass) -->
    <div class="fixed top-0 left-0 w-full h-1 bg-gray-100 z-[200]">
        <div class="h-full bg-primary transition-all duration-300" style="width: <%= auction.stockSoldPercent %>%"></div>
    </div>

    <!-- Top Urgent Banner (Matches Screenshot) -->
    <div class="bg-red-500/90 backdrop-blur-sm py-2 text-white border-b border-red-600/20 z-[150] sticky top-0">
        <div class="container mx-auto px-4 flex items-center justify-center gap-3">
            <i class="fas fa-fire text-yellow-300 animate-pulse"></i>
            <span class="text-[11px] font-black uppercase tracking-wider">Sirf Ended bache hain! Abhi bid karein — jitni deri, utna mehenga.</span>
        </div>
    </div>

    <!-- Navigation Header (Matches Screenshot) -->
    <header class="bg-white border-b border-gray-100 z-[140] sticky top-[33px]">
        <div class="container mx-auto px-4 h-20 flex justify-between items-center">
            <!-- Logo Section -->
            <a href="/auctions" class="flex items-center gap-2">
                <div class="w-10 h-10 bg-gray-50 flex items-center justify-center rounded-xl overflow-hidden grayscale opacity-80">
                    <i class="fas fa-gavel text-secondary"></i>
                </div>
                <div class="flex flex-col -space-y-1">
                    <span class="text-2xl font-black tracking-tighter uppercase font-heading">Boli<span class="text-primary italic">Bazar</span></span>
                </div>
            </a>

            <!-- Nav Links -->
            <nav class="hidden lg:flex items-center gap-10">
                <a href="/auctions" class="flex items-center gap-2 text-[10px] font-black uppercase tracking-widest text-gray-500 hover:text-primary transition">
                    <i class="fas fa-bolt-lightning text-gray-300"></i> Live Auctions
                </a>
                <a href="/auctions" class="flex items-center gap-2 text-[10px] font-black uppercase tracking-widest text-gray-500 hover:text-primary transition">
                    <i class="far fa-clock text-gray-300"></i> Upcoming Lots
                </a>
                <a href="/dashboard" class="flex items-center gap-2 text-[10px] font-black uppercase tracking-widest text-primary transition">
                    <i class="fas fa-gavel"></i> Dashboard
                </a>
            </nav>

            <!-- User Area -->
            <div class="flex items-center gap-6">
                <!-- Tokens -->
                <div class="bg-red-50 border border-red-100 px-4 py-2 rounded-xl flex items-center gap-3">
                    <i class="fas fa-wallet text-primary text-xs"></i>
                    <span class="text-[10px] font-black uppercase text-secondary">₹2,450 <span class="text-primary">Tokens</span></span>
                </div>
                <!-- Notifs -->
                <div class="relative">
                    <div class="w-10 h-10 rounded-xl bg-gray-50 flex items-center justify-center text-secondary relative hover:bg-gray-100 cursor-pointer transition">
                        <i class="far fa-bell"></i>
                        <span class="absolute -top-1 -right-1 w-4 h-4 bg-primary text-white text-[8px] font-black flex items-center justify-center rounded-full border-2 border-white">1</span>
                    </div>
                </div>
                <!-- User -->
                <a href="/dashboard" class="flex items-center gap-3 pl-2 border-l border-gray-100 group cursor-pointer relative z-[1001]">
                    <div class="w-10 h-10 rounded-xl bg-primary text-white flex items-center justify-center font-black text-sm shadow-lg shadow-red-500/20 group-hover:scale-105 transition">
                        <%= (user ? user.username.charAt(0) : 'R').toUpperCase() %>
                    </div>
                    <div class="hidden sm:flex flex-col">
                        <span class="text-[10px] font-black text-secondary uppercase tracking-widest group-hover:text-primary transition"><%= user ? user.username : 'Retailer' %></span>
                        <span class="text-[8px] font-bold text-gray-400">View Account</span>
                    </div>
                </a>
            </div>
        </div>
    </header>

    <main class="container mx-auto px-4 mt-8">
        
        <!-- Breadcrumbs -->
        <div class="flex items-center gap-3 text-[10px] font-black uppercase tracking-widest text-gray-400 mb-8">
            <a href="/auctions" class="flex items-center gap-2 hover:text-primary transition">
                <i class="fas fa-arrow-left text-[8px]"></i> Live Auctions
            </a>
            <i class="fas fa-chevron-right text-[7px] opacity-30"></i>
            <span><%= auction.category || 'Grains & Cereals' %></span>
            <i class="fas fa-chevron-right text-[7px] opacity-30"></i>
            <span class="text-secondary opacity-60"><%= auction.title %></span>
        </div>

        <div class="grid grid-cols-1 lg:grid-cols-12 gap-8 items-start">
            
            <!-- LEFT COLUMN (7/12) -->
            <div class="lg:col-span-8 space-y-8 pb-12">
                
                <!-- Product Banner (Matches Screenshot Overlay Style) -->
                <div class="relative rounded-[2.5rem] overflow-hidden aspect-[16/9] lg:aspect-video shadow-2xl group">
                    <img src="<%= auction.imageUrl || '/lot1.png' %>" class="w-full h-full object-cover group-hover:scale-105 transition duration-[2s]">
                    
                    <!-- Top Labels -->
                    <div class="absolute top-8 left-8 flex gap-3">
                        <div class="px-4 py-2 bg-accent text-secondary rounded-xl text-[10px] font-black uppercase tracking-widest flex items-center gap-2 shadow-xl">
                            <i class="fas fa-star"></i> Featured
                        </div>
                        <div class="px-4 py-2 bg-white/90 backdrop-blur-md text-secondary rounded-xl text-[10px] font-black uppercase tracking-widest shadow-xl">
                            <%= auction.category || 'Grains & Cereals' %>
                        </div>
                    </div>

                    <!-- Right Status (Countdown Look) -->
                    <div class="absolute top-8 right-8">
                        <div class="px-4 py-2 bg-[#b91c1c] text-white rounded-xl text-[11px] font-black uppercase tracking-wider flex items-center gap-2 shadow-2xl">
                            <i class="far fa-clock"></i> 03:39
                        </div>
                    </div>

                    <!-- Bottom Info Overlay -->
                    <div class="absolute bottom-0 left-0 w-full p-12 bg-gradient-to-t from-black/80 via-black/40 to-transparent">
                        <h2 class="text-4xl font-black text-white font-heading leading-tight mb-2"><%= auction.title %></h2>
                        <span class="text-xs font-bold text-white/60 uppercase tracking-widest">SKU: <%= (auction.category.slice(0,2) + '-' + auction.title.slice(0,4)).toUpperCase() %>-<%= auction._id.toString().slice(-4).toUpperCase() %></span>
                    </div>
                </div>


                


                <!-- Tabs & Content -->
                <div class="bg-white rounded-[2.5rem] border border-gray-100 shadow-sm overflow-hidden">
                    <!-- Tab Headers -->
                    <div class="px-8 pt-6 border-b border-gray-100 flex gap-8">
                        <button class="tab-btn active text-xs font-black uppercase tracking-widest" onclick="switchTab('overview')">Overview</button>
                        <button class="tab-btn text-xs font-black uppercase tracking-widest" onclick="switchTab('specs')">Specifications</button>
                        <button class="tab-btn text-xs font-black uppercase tracking-widest" onclick="switchTab('history')">Bid Activity</button>
                    </div>
                    
                    <!-- Tab Panels -->
                    <div class="p-8">
                        <div id="overview-tab" class="tab-panel">
                            <h4 class="text-lg font-black mb-4">Product Overview</h4>
                            <p class="text-sm text-gray-500 leading-relaxed mb-10">
                                <%= auction.description || 'Premium factory-direct lot. No middleman — sourced directly from the manufacturer for maximum retail margins. Inspected and verified by BoliBazar quality team.' %>
                            </p>
                            
                            <!-- 4-Card Mini Stats Grid (Exact Parity) -->
                            <div class="grid grid-cols-2 md:grid-cols-4 gap-4 mb-10">
                                <div class="bg-[#fafafa] p-5 rounded-[2rem] border border-gray-100 flex flex-col items-center text-center">
                                    <div class="w-10 h-10 rounded-xl bg-white flex items-center justify-center text-gray-400 mb-3 shadow-sm">
                                        <i class="fas fa-tag text-xs"></i>
                                    </div>
                                    <p class="text-[9px] font-black text-gray-400 uppercase tracking-widest mb-1">Starting Price</p>
                                    <p class="text-sm font-black text-secondary">₹1,200</p>
                                </div>
                                <div class="bg-[#fafafa] p-5 rounded-[2rem] border border-gray-100 flex flex-col items-center text-center">
                                    <div class="w-10 h-10 rounded-xl bg-white flex items-center justify-center text-primary mb-3 shadow-sm">
                                        <i class="fas fa-chart-line text-xs"></i>
                                    </div>
                                    <p class="text-[9px] font-black text-gray-400 uppercase tracking-widest mb-1">Price Hike</p>
                                    <p class="text-sm font-black text-primary">+9.19%</p>
                                </div>
                                <div class="bg-[#fafafa] p-5 rounded-[2rem] border border-gray-100 flex flex-col items-center text-center">
                                    <div class="w-10 h-10 rounded-xl bg-white flex items-center justify-center text-indigo-600 mb-3 shadow-sm">
                                        <i class="fas fa-users text-xs"></i>
                                    </div>
                                    <p class="text-[9px] font-black text-gray-400 uppercase tracking-widest mb-1">Total Buyers</p>
                                    <p class="text-sm font-black text-indigo-600">9</p>
                                </div>
                                <div class="bg-[#fafafa] p-5 rounded-[2rem] border border-gray-100 flex flex-col items-center text-center">
                                    <div class="w-10 h-10 rounded-xl bg-white flex items-center justify-center text-orange-600 mb-3 shadow-sm">
                                        <i class="fas fa-cubes text-xs"></i>
                                    </div>
                                    <p class="text-[9px] font-black text-gray-400 uppercase tracking-widest mb-1">Remaining</p>
                                    <p class="text-sm font-black text-orange-600">41</p>
                                </div>
                            </div>

                            <!-- Mini Gallery Grid -->
                            <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
                                <div class="aspect-square bg-gray-50 rounded-2xl overflow-hidden border border-gray-100 group">
                                    <img src="<%= auction.imageUrl || '/lot1.png' %>" class="w-full h-full object-cover group-hover:scale-110 transition duration-500">
                                </div>
                            </div>
                        </div>

                        <div id="specs-tab" class="tab-panel hidden">
                            <h4 class="text-lg font-black mb-4">Technical Specifications</h4>
                            <div class="space-y-4">
                                <div class="flex justify-between py-3 border-b border-gray-50">
                                    <span class="text-xs font-bold text-gray-400 uppercase">Lot Weight / Unit</span>
                                    <span class="text-xs font-black text-secondary">50 KG Bag</span>
                                </div>
                                <div class="flex justify-between py-3 border-b border-gray-50">
                                    <span class="text-xs font-bold text-gray-400 uppercase">Minimum Order</span>
                                    <span class="text-xs font-black text-secondary"><%= auction.moq || 1 %> Bag</span>
                                </div>
                                <div class="flex justify-between py-3 border-b border-gray-50">
                                    <span class="text-xs font-bold text-gray-400 uppercase">GST Bracket</span>
                                    <span class="text-xs font-black text-secondary">5% Applicable</span>
                                </div>
                            </div>
                        </div>

                        <div id="history-tab" class="tab-panel hidden">
                            <h4 class="text-lg font-black mb-6">Real-Time Bidding Activity</h4>
                            <div class="space-y-4 max-h-[400px] overflow-y-auto pr-2 no-scrollbar">
                                <% (locals.bidHistory || []).forEach(bid => { %>
                                    <div class="flex items-center justify-between p-4 bg-gray-50 rounded-2xl border border-gray-100">
                                        <div class="flex items-center gap-4">
                                            <div class="w-10 h-10 rounded-full bg-white flex items-center justify-center text-primary font-black shadow-sm">
                                                <%= bid.bidder.charAt(0) %>
                                            </div>
                                            <div>
                                                <p class="text-xs font-black text-secondary"><%= bid.bidder %></p>
                                                <p class="text-[9px] font-bold text-gray-400"><%= bid.time %></p>
                                            </div>
                                        </div>
                                        <div class="text-right">
                                            <p class="text-sm font-black text-secondary">₹<%= bid.amount.toLocaleString() %></p>
                                            <p class="text-[9px] font-bold text-primary"><%= bid.quantity %> Bags</p>
                                        </div>
                                    </div>
                                <% }); %>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Price Tracker Section -->
                <div class="bg-secondary rounded-[2.5rem] p-8 text-white relative overflow-hidden">
                    <div class="absolute top-0 right-0 p-8">
                        <div class="px-4 py-1.5 bg-white/10 rounded-full text-[10px] font-black uppercase tracking-widest backdrop-blur-md">Price Trend</div>
                    </div>
                    <h4 class="text-xl font-black mb-1">Live Price Tracker</h4>
                    <p class="text-xs text-white/50 mb-10">Real-time demand visualization</p>
                    
                    <!-- Graph Mockup (SVG) -->
                    <div class="w-full h-48 mt-4 relative">
                        <svg class="w-full h-full" viewBox="0 0 1000 200" preserveAspectRatio="none">
                            <path d="M0,180 Q250,170 400,120 T700,80 T1000,20" fill="none" stroke="#d90429" stroke-width="6" stroke-linecap="round"></path>
                            <path d="M0,180 Q250,170 400,120 T700,80 T1000,20 L1000,200 L0,200 Z" fill="url(#grad1)" opacity="0.2"></path>
                            <defs>
                                <linearGradient id="grad1" x1="0%" y1="0%" x2="0%" y2="100%">
                                    <stop offset="0%" style="stop-color:#d90429;stop-opacity:1" />
                                    <stop offset="100%" style="stop-color:#d90429;stop-opacity:0" />
                                </linearGradient>
                            </defs>
                        </svg>
                        <div class="absolute bottom-0 left-0 w-full flex justify-between px-2 text-[9px] font-bold text-white/30 uppercase tracking-widest mt-4">
                            <span>Started (₹<%= auction.initialPrice %>)</span>
                            <span>Highest (₹<%= auction.currentBid %>)</span>
                        </div>
                    </div>
                </div>

                <!-- Compact Real-Time Leaderboard Section -->
                <div class="bg-white rounded-[2.5rem] p-6 border border-gray-100 shadow-xl mt-8">
                    <div class="flex items-center justify-between mb-6">
                        <div class="flex items-center gap-3">
                            <div class="w-8 h-8 rounded-lg bg-yellow-50 flex items-center justify-center text-yellow-500">
                                <i class="fas fa-trophy text-sm"></i>
                            </div>
                            <div>
                                <h4 class="text-base font-black text-secondary uppercase tracking-tight">Top Bidders</h4>
                                <p class="text-[9px] font-bold text-gray-400 uppercase tracking-widest">Live ranking — Updates instantly</p>
                            </div>
                        </div>
                        <div class="flex items-center gap-2 text-emerald-500">
                            <i class="fas fa-circle text-[6px] animate-pulse"></i>
                            <span class="text-[9px] font-black uppercase tracking-widest">Live Activity</span>
                        </div>
                    </div>

                    <!-- Compact Dynamic List -->
                    <div id="product-leaderboard-container" class="space-y-3 min-h-[100px]">
                        <!-- Loading State -->
                        <div class="flex flex-col items-center justify-center py-6 opacity-30">
                            <i class="fas fa-spinner fa-spin text-lg mb-2"></i>
                            <p class="text-[9px] font-black uppercase">Fetching Bidders...</p>
                        </div>
                    </div>
                </div>

                <script>
                    async function refreshProductLeaderboard() {
                        try {
                            const res = await fetch(`/api/auctions/<%= auction._id %>/leaderboard`);
                            const data = await res.json();
                            const container = document.getElementById('product-leaderboard-container');
                            
                            if (data.length === 0) {
                                container.innerHTML = `
                                    <div class="flex flex-col items-center justify-center py-8 text-gray-300">
                                        <i class="fas fa-users text-2xl mb-2"></i>
                                        <p class="text-[10px] font-black uppercase tracking-widest">No bids yet on this lot</p>
                                    </div>`;
                                return;
                            }

                            container.innerHTML = data.map((item, index) => `
                                <div class="flex items-center justify-between p-4 bg-gray-50/50 hover:bg-gray-50 rounded-2xl transition border border-transparent hover:border-gray-100">
                                    <div class="flex items-center gap-4">
                                        <div class="w-8 h-8 rounded-full bg-white border border-gray-100 flex items-center justify-center text-[10px] font-black shadow-sm ${index === 0 ? 'text-yellow-600 border-yellow-200 bg-yellow-50/30' : 'text-gray-400'}">
                                            #${index + 1}
                                        </div>
                                        <div>
                                            <p class="text-xs font-black text-secondary uppercase tracking-tight">${item.username}</p>
                                            <p class="text-[9px] font-bold text-gray-400 uppercase tracking-widest">${item.city || 'Retailer'}</p>
                                        </div>
                                    </div>
                                    <div class="text-right">
                                        <p class="text-xs font-black text-secondary">₹${item.totalAmount.toLocaleString()}</p>
                                        <p class="text-[9px] font-bold text-primary uppercase tracking-tighter">${item.totalQuantity} Units</p>
                                    </div>
                                </div>
                            `).join('');
                        } catch (err) {
                            console.error('Leaderboard Refresh Error:', err);
                        }
                    }

                    // Initial Load
                    refreshProductLeaderboard();
                    // Auto-update every 10 seconds
                    setInterval(refreshProductLeaderboard, 10000);
                </script>
            </div>

            <!-- RIGHT COLUMN: STICKY SIDEBAR (5/12) -->
            <div class="lg:col-span-4 sticky-sidebar">
                
                <!-- Live Price Tracker Card (Moved to Sidebar) -->
                <div class="bg-white rounded-[2.5rem] p-8 border border-gray-100 shadow-[0_20px_60px_rgba(217,4,41,0.12)] mb-6">
                    <div class="flex items-center justify-between mb-8">
                        <h4 class="text-xl font-black text-secondary">Live Price Tracker</h4>
                        <div class="flex items-center gap-2 text-emerald-500">
                            <div class="w-2.5 h-2.5 rounded-full bg-emerald-500 animate-pulse"></div>
                            <span class="text-[11px] font-black uppercase tracking-widest">Real-time</span>
                        </div>
                    </div>

                    <!-- 2x2 Grid -->
                    <div class="grid grid-cols-2 gap-4 mb-8">
                        <!-- Starting Price -->
                        <div class="bg-[#f8fafc] p-4 rounded-[2rem] border border-slate-100 overflow-hidden">
                            <p class="text-[9px] font-black text-slate-400 uppercase tracking-widest mb-1">Start</p>
                            <p class="text-lg font-black text-slate-600">₹<%= auction.initialPrice.toLocaleString() %></p>
                        </div>
                        <!-- Current Price -->
                        <div class="bg-[#fff1f2] p-4 rounded-[2rem] border border-red-50 overflow-hidden">
                            <p class="text-[9px] font-black text-red-400 uppercase tracking-widest mb-1">Current</p>
                            <p class="text-lg font-black text-[#9d174d]">₹<%= auction.currentBid.toLocaleString() %></p>
                        </div>
                        <!-- Total Buyers -->
                        <div class="bg-[#eff6ff] p-4 rounded-[2rem] border border-blue-50 overflow-hidden">
                            <p class="text-[9px] font-black text-blue-400 uppercase tracking-widest mb-1">Buyers</p>
                            <p class="text-lg font-black text-indigo-600"><%= auction.totalBuyers %></p>
                        </div>
                        <!-- Remaining Stock -->
                        <div class="bg-[#fff7ed] p-4 rounded-[2rem] border border-orange-50 overflow-hidden">
                            <p class="text-[9px] font-black text-orange-400 uppercase tracking-widest mb-1">Stock</p>
                            <p class="text-lg font-black text-[#c2410c]"><%= auction.stockRemaining %></p>
                        </div>
                    </div>

                    <!-- Stock Sold Section -->
                    <div class="space-y-3">
                        <div class="flex justify-between items-end">
                            <span class="text-[10px] font-bold text-gray-400">Stock Sold</span>
                            <span class="text-[10px] font-black text-[#b91c1c] uppercase tracking-widest"><%= auction.stockSoldPercent %>% GONE</span>
                        </div>
                        <div class="h-3 w-full bg-gray-100 rounded-full overflow-hidden">
                            <div class="h-full bg-primary rounded-full transition-all duration-1000" style="width: <%= auction.stockSoldPercent %>%"></div>
                        </div>
                    </div>

                    <!-- Price Hike Footer -->
                    <div class="mt-6 bg-[#fefce8] p-4 rounded-2xl border border-yellow-100 flex justify-between items-center">
                        <span class="text-[10px] font-black text-yellow-800 uppercase tracking-widest">Hike</span>
                        <span class="text-base font-black text-yellow-700 tracking-tighter">+<%= auction.hikePercentage %>%</span>
                    </div>
                </div>
                <div class="bg-white rounded-[2.5rem] border border-gray-100 shadow-2xl shadow-red-900/5 relative overflow-hidden">
                    
                    <!-- Sidebar Header (Matches Screenshot) -->
                    <div class="p-8 border-b border-gray-50">
                        <div class="flex items-center gap-3 mb-2">
                           <h4 class="text-2xl font-black text-secondary">Bid Lagao</h4>
                        </div>
                    </div>

                    <div class="p-8 space-y-8">
                        <!-- Quantity Input Area -->
                        <div class="space-y-4">
                            <label class="text-[10px] font-black text-gray-400 uppercase tracking-widest block ml-2">Quantity (Bags)</label>
                            
                            <!-- MOQ Banner (Yellow) -->
                            <div class="bg-orange-50/50 px-4 py-3 rounded-xl border border-orange-100 flex items-center gap-3 text-orange-700">
                                <div class="w-5 h-5 rounded-lg bg-orange-100 flex items-center justify-center text-[10px]">
                                    <i class="fas fa-box-open"></i>
                                </div>
                                <span class="text-[10px] font-black uppercase tracking-widest">Min. Order Qty (MOQ): <%= auction.moq || 5 %> bags</span>
                            </div>

                            <div class="flex items-center gap-4">
                                <button onclick="adjustQty(-1)" class="w-12 h-12 rounded-xl bg-gray-50 hover:bg-gray-100 flex items-center justify-center text-lg font-black transition border border-gray-100">
                                    <i class="fas fa-minus"></i>
                                </button>
                                <div class="grow relative">
                                    <input type="number" id="bidQty" min="<%= auction.moq || 1 %>" value="<%= auction.moq || 5 %>" class="qty-input w-full bg-white border border-gray-100 rounded-xl px-6 py-3 text-2xl font-black text-center focus:outline-none focus:border-primary transition" oninput="calculateTotal()">
                                </div>
                                <button onclick="adjustQty(1)" class="w-12 h-12 rounded-xl bg-gray-50 hover:bg-gray-100 flex items-center justify-center text-lg font-black transition border border-gray-100">
                                    <i class="fas fa-plus"></i>
                                </button>
                            </div>

                            <!-- Centered Quick Chips -->
                            <div class="grid grid-cols-4 gap-2 pt-2">
                                <button onclick="setQty(5)" class="qty-chip py-3 rounded-xl bg-primary text-xs font-black text-white shadow-lg shadow-red-500/10 transition-all" data-qty="5">5</button>
                                <button onclick="setQty(10)" class="qty-chip py-3 rounded-xl bg-gray-50 text-xs font-bold text-gray-400 hover:bg-gray-100 transition" data-qty="10">10</button>
                                <button onclick="setQty(20)" class="qty-chip py-3 rounded-xl bg-gray-50 text-xs font-bold text-gray-400 hover:bg-gray-100 transition" data-qty="20">20</button>
                                <button onclick="setQty(50)" class="qty-chip py-3 rounded-xl bg-gray-50 text-xs font-bold text-gray-400 hover:bg-gray-100 transition" data-qty="50">50</button>
                            </div>
                        </div>

                        <!-- Payment Mode Toggles -->
                        <div class="space-y-4">
                            <label class="text-[10px] font-black text-gray-400 uppercase tracking-widest block ml-2">Payment Mode</label>
                            <div class="grid grid-cols-2 gap-3">
                                <div onclick="setPaymentMode('full')" id="pay-full" class="payment-card border-2 border-gray-100 rounded-2xl p-4 cursor-pointer transition hover:bg-gray-50 group active">
                                    <div class="flex items-center gap-2 mb-1">
                                        <i class="fas fa-truck-fast text-xs text-gray-400 group-hover:text-primary transition"></i>
                                        <span class="text-[10px] font-black uppercase text-secondary">Full Payment</span>
                                    </div>
                                    <p class="text-[10px] font-black text-emerald-500">FREE Delivery</p>
                                    <p class="text-[8px] font-bold text-gray-400 mt-1">100% amount pay karein</p>
                                </div>
                                <div onclick="setPaymentMode('cod')" id="pay-cod" class="payment-card border-2 border-gray-100 rounded-2xl p-4 cursor-pointer transition hover:bg-gray-50 group">
                                    <div class="flex items-center gap-2 mb-1">
                                        <i class="fas fa-box text-xs text-gray-400 group-hover:text-primary transition"></i>
                                        <span class="text-[10px] font-black uppercase text-secondary">COD</span>
                                    </div>
                                    <p class="text-[10px] font-black text-orange-500">+10% Delivery Fee</p>
                                    <p class="text-[8px] font-bold text-gray-400 mt-1">90% balance on delivery</p>
                                </div>
                            </div>
                        </div>

                        <!-- Detailed Price Breakdown (Card Style) -->
                        <div class="bg-gray-50/50 rounded-2xl p-6 space-y-3">
                            <div class="flex justify-between items-center text-[10px]">
                                <span class="font-bold text-gray-400 uppercase">Current Price × <span id="display-qty">5</span> bags</span>
                                <span class="font-black text-secondary" id="display-subtotal">₹0</span>
                            </div>
                            <div class="flex justify-between items-center text-[10px]">
                                <span class="font-bold text-gray-400 uppercase">Token Advance (10%)</span>
                                <span class="font-black text-primary" id="display-advance">₹0</span>
                            </div>
                            <div id="cod-row-balance" class="hidden flex justify-between items-center text-[10px]">
                                <span class="font-bold text-gray-400 uppercase">COD Balance (90%)</span>
                                <span class="font-black text-secondary" id="display-balance">₹0</span>
                            </div>
                            <div id="cod-row-fee" class="hidden flex justify-between items-center text-[10px]">
                                <span class="font-bold text-orange-400 uppercase">Delivery Fee (10% extra)</span>
                                <span class="font-black text-orange-500" id="display-fee">₹0</span>
                            </div>
                            <div class="pt-3 border-t border-gray-100 flex justify-between items-center">
                                <span class="text-[11px] font-black text-secondary uppercase tracking-widest">Total Payable</span>
                                <span class="text-xl font-black text-secondary" id="billing-total-payable">₹0</span>
                            </div>
                        </div>

                        <!-- Token Balance Card with Status -->
                        <div class="bg-red-50/50 border border-red-100 rounded-2xl p-5 flex items-center justify-between">
                            <div class="flex items-center gap-3">
                                <div class="w-10 h-10 rounded-xl bg-white flex items-center justify-center text-primary shadow-sm">
                                    <i class="fas fa-wallet"></i>
                                </div>
                                <div>
                                    <h5 class="text-[10px] font-black text-secondary uppercase">Shopping Coins</h5>
                                    <p class="text-[9px] font-bold text-gray-500">Available: ₹<%= (user && typeof user.walletBalance !== 'undefined') ? user.walletBalance.toLocaleString() : '0' %></p>
                                </div>
                            </div>
                            <div class="text-right">
                                <p class="text-[9px] font-black text-emerald-500 uppercase flex items-center gap-1">
                                    <i class="fas fa-check-circle text-[8px]"></i>
                                    Verified Account
                                </p>
                            </div>
                        </div>

                        <!-- Main Primary CTA (Updated for Checkout) -->
                        <div class="pt-2">
                            <button onclick="openCheckoutModal()" class="w-full bg-gradient-to-r from-[#d90429] to-[#ef233c] hover:opacity-90 text-white py-5 rounded-2xl text-[12px] font-black uppercase tracking-[0.2em] shadow-xl shadow-red-500/20 active:scale-95 transition-all flex items-center justify-center gap-4">
                                <i class="fas fa-bolt-lightning"></i>
                                Abhi Bid Lagao
                            </button>
                        </div>
                    </div>
                </div>

                <!-- Payment & Delivery Policy Card -->
                <div class="bg-white rounded-[2rem] border border-gray-100 shadow-xl overflow-hidden mt-6">
                    <div class="p-6 border-b border-gray-50 flex items-center gap-3">
                        <div class="w-8 h-8 rounded-lg bg-red-50 flex items-center justify-center text-primary">
                            <i class="fas fa-shield-heart text-sm"></i>
                        </div>
                        <h4 class="text-sm font-black text-secondary uppercase tracking-widest">Payment & Delivery Policy</h4>
                    </div>
                    <div class="p-6 space-y-4">
                        <!-- 10% Advance -->
                        <div class="bg-[#eff6ff] p-4 rounded-2xl border border-blue-100 flex items-center gap-4 group hover:bg-blue-100 transition">
                            <div class="w-10 h-10 rounded-xl bg-white flex items-center justify-center text-blue-600 shadow-sm transition-transform group-hover:scale-110">
                                <i class="fas fa-credit-card text-xs"></i>
                            </div>
                            <div>
                                <h5 class="text-xs font-black text-secondary">10% Advance Mandatory</h5>
                                <p class="text-[9px] font-bold text-gray-500 mt-0.5">Token balance se 10% advance kat jaata hai — booking confirm.</p>
                            </div>
                        </div>
                        <!-- Full Payment -->
                        <div class="bg-[#f0fdf4] p-4 rounded-2xl border border-green-100 flex items-center gap-4 group hover:bg-green-100 transition">
                            <div class="w-10 h-10 rounded-xl bg-white flex items-center justify-center text-green-600 shadow-sm transition-transform group-hover:scale-110">
                                <i class="fas fa-truck-fast text-xs"></i>
                            </div>
                            <div>
                                <h5 class="text-xs font-black text-secondary">Full Payment = FREE Delivery</h5>
                                <p class="text-[9px] font-bold text-gray-500 mt-0.5">100% amount pay karein toh delivery bilkul free milegi.</p>
                            </div>
                        </div>
                        <!-- COD -->
                        <div class="bg-[#fffbeb] p-4 rounded-2xl border border-yellow-100 flex items-center gap-4 group hover:bg-yellow-100 transition">
                            <div class="w-10 h-10 rounded-xl bg-white flex items-center justify-center text-yellow-600 shadow-sm transition-transform group-hover:scale-110">
                                <i class="fas fa-box text-xs"></i>
                            </div>
                            <div>
                                <h5 class="text-xs font-black text-secondary">COD = 10% Extra Charge</h5>
                                <p class="text-[9px] font-bold text-gray-500 mt-0.5">90% balance delivery pe + 10% extra delivery fee lagegi.</p>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Smart Notifications Section -->
                <div class="bg-red-700 rounded-[2rem] p-8 text-white mt-6 relative overflow-hidden shadow-2xl shadow-red-900/40 group">
                    <div class="absolute -top-4 -right-4 w-24 h-24 bg-white/10 rounded-full blur-2xl group-hover:scale-150 transition-transform duration-700"></div>
                    
                    <div class="relative z-10">
                        <div class="flex items-center gap-3 mb-4">
                            <i class="fas fa-bell animate-bounce"></i>
                            <h4 class="text-sm font-black uppercase tracking-widest">Smart Notifications</h4>
                        </div>
                        <p class="text-[11px] font-medium leading-relaxed mb-8 text-white/80">
                            Jab is lot mein sirf 5 minute bache honge, hum aapko push notification bhejenge. Koi bid miss mat karo!
                        </p>
                        
                        <button class="w-full bg-white text-red-700 py-4 rounded-2xl text-[10px] font-black uppercase tracking-widest shadow-xl hover:bg-gray-50 active:scale-95 transition-all flex items-center justify-center gap-3">
                            <i class="fas fa-clock"></i>
                            5 Min Alert Set Karein
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <!-- Pixel Perfect Bid Notification (Matches Screenshot) -->
    <div class="fixed bottom-8 right-8 z-[200]" id="bid-toast">
        <div class="bg-[#eff6ff] border border-blue-200 rounded-3xl p-6 shadow-2xl flex items-center gap-5 max-w-[450px] animate-slide-up">
            <div class="w-14 h-14 rounded-full bg-blue-100 flex items-center justify-center text-blue-600 shadow-sm">
                <i class="fas fa-info-circle text-xl"></i>
            </div>
            <div class="flex-grow">
                <p class="text-[14px] font-medium text-blue-800 leading-tight">
                    🔥 <span class="font-bold">Rajiv Nair (Kochi)</span> ne bid kiya — price <span class="font-bold text-blue-900">₹1,310.29</span> ho gaya!
                </p>
            </div>
        </div>
    </div>

    <!-- Premium Checkout Modal -->
    <div id="checkoutModal" class="fixed inset-0 z-[3000] hidden">
        <!-- Backdrop -->
        <div class="absolute inset-0 bg-black/60 backdrop-blur-sm" onclick="closeCheckoutModal()"></div>
        
        <!-- Modal Content -->
        <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-full max-w-md bg-white rounded-[3rem] p-10 shadow-2xl animate-slide-up">
            <div class="text-center mb-10">
                <div class="w-20 h-20 bg-red-50 rounded-3xl flex items-center justify-center mx-auto mb-6 text-primary shadow-inner">
                    <i class="fas fa-shopping-bag text-3xl"></i>
                </div>
                <h3 class="text-2xl font-black text-secondary font-heading">Confirm Booking</h3>
                <p class="text-xs text-gray-400 font-bold uppercase tracking-widest mt-2">Product: <%= auction.title %></p>
            </div>

            <!-- Order Summary -->
            <div class="bg-gray-50 rounded-[2rem] p-8 space-y-6 mb-10">
                <div class="flex justify-between items-center">
                    <span class="text-[10px] font-black text-gray-400 uppercase">Quantity Selected</span>
                    <span class="text-xs font-black text-secondary" id="modal-qty">5 Bags</span>
                </div>
                <div class="flex justify-between items-center">
                    <span class="text-[10px] font-black text-gray-400 uppercase">Payment Mode</span>
                    <span class="text-xs font-black text-secondary uppercase" id="modal-payment-mode">Full Payment</span>
                </div>
                <div class="pt-6 border-t border-gray-200 flex justify-between items-center">
                    <span class="text-xs font-black text-secondary uppercase">Total Payable</span>
                    <span class="text-2xl font-black text-primary" id="modal-total">₹0</span>
                </div>
                <!-- Reward Badge -->
                <div class="pt-4 flex justify-center">
                    <div class="bg-green-100 text-green-700 px-4 py-2 rounded-full text-[9px] font-black uppercase tracking-tighter flex items-center gap-2">
                        <i class="fas fa-gift"></i>
                        Aapko <span id="modal-award">₹0</span> Coins milenge!
                    </div>
                </div>
            </div>

            <!-- Actions -->
            <div class="space-y-4">
                <button id="confirm-booking-btn" onclick="processBooking()" class="w-full bg-primary text-white py-5 rounded-2xl text-[12px] font-black uppercase tracking-widest hover:opacity-90 active:scale-95 transition-all shadow-xl flex items-center justify-center gap-3">
                    <span id="btn-text">Confirm & Pay</span>
                    <i class="fas fa-arrow-right" id="btn-icon"></i>
                </button>
                <button onclick="closeCheckoutModal()" class="w-full py-4 text-[10px] font-black text-gray-400 uppercase hover:text-secondary transition">
                    Abhi Nahi
                </button>
            </div>
        </div>
    </div>

    <script>
        const currentPrice = <%= auction.currentBid %>;
        let paymentMode = 'full';

        function openCheckoutModal() {
            const qty = document.getElementById('bidQty').value;
            const total = document.getElementById('billing-total-payable').textContent;
            
            document.getElementById('modal-qty').textContent = qty + ' Bags';
            document.getElementById('modal-payment-mode').textContent = paymentMode === 'full' ? 'Full Payment' : 'Cash on Delivery';
            document.getElementById('modal-total').textContent = total;
            document.getElementById('modal-award').textContent = total;
            
            document.getElementById('checkoutModal').classList.remove('hidden');
            document.body.style.overflow = 'hidden';
        }

        function closeCheckoutModal() {
            document.getElementById('checkoutModal').classList.add('hidden');
            document.body.style.overflow = 'auto';
        }

        async function processBooking() {
            const btn = document.getElementById('confirm-booking-btn');
            const btnText = document.getElementById('btn-text');
            const btnIcon = document.getElementById('btn-icon');
            const qty = document.getElementById('bidQty').value;
            
            // Loading State
            btn.disabled = true;
            btnText.textContent = 'Processing Payment...';
            btnIcon.className = 'fas fa-spinner fa-spin';
            btn.classList.add('opacity-80');

            try {
                const response = await fetch('/api/checkout/<%= auction._id %>', {
                    method: 'POST',
                    headers: { 
                        'Content-Type': 'application/json',
                        'Cache-Control': 'no-cache'
                    },
                    body: JSON.stringify({
                        quantity: qty,
                        paymentMode: paymentMode
                    })
                });

                // Check for non-OK responses
                if (!response.ok) {
                    const errorText = await response.text();
                    let errMsg = `V5-Error (${response.status})`;
                    try {
                        const errData = JSON.parse(errorText);
                        errMsg = errData.error || errMsg;
                    } catch(e) {
                        errMsg += ': ' + errorText.substring(0, 100);
                    }
                    alert('Booking Failed: ' + errMsg);
                    resetBtn(btn, btnText, btnIcon);
                    return;
                }

                const data = await response.json();

                if (data.success) {
                    btn.classList.add('bg-green-600');
                    btn.classList.remove('bg-primary');
                    btnText.textContent = 'Booking Confirmed!';
                    btnIcon.className = 'fas fa-check-circle';
                    
                    // Delay for emotional impact
                    setTimeout(() => {
                        window.location.href = data.redirect;
                    }, 1500);
                } else {
                    alert('Booking Failed: ' + (data.error || 'Unknown error'));
                    resetBtn(btn, btnText, btnIcon);
                }
            } catch (err) {
                console.error('Checkout error:', err);
                alert('Connection Error (V5): The request could not be completed. Details: ' + err.message);
                resetBtn(btn, btnText, btnIcon);
            }
        }

        function resetBtn(btn, text, icon) {
            btn.disabled = false;
            text.textContent = 'Confirm & Pay';
            icon.className = 'fas fa-arrow-right';
            btn.classList.remove('opacity-80');
        }

        function switchTab(tabName) {
            document.querySelectorAll('.tab-btn').forEach(btn => btn.classList.remove('active'));
            event.target.classList.add('active');
            document.querySelectorAll('.tab-panel').forEach(panel => panel.classList.add('hidden'));
            document.getElementById(tabName + '-tab').classList.remove('hidden');
        }

        function adjustQty(delta) {
            const input = document.getElementById('bidQty');
            let val = (parseInt(input.value) || 0) + delta;
            if (val < <%= auction.moq || 1 %>) val = <%= auction.moq || 1 %>;
            input.value = val;
            calculateTotal();
        }

        function setQty(val) {
            document.getElementById('bidQty').value = val;
            calculateTotal();
        }

        function setPaymentMode(mode) {
            paymentMode = mode;
            document.querySelectorAll('.payment-card').forEach(card => card.classList.remove('active', 'border-primary', 'bg-red-50/20'));
            document.getElementById('pay-' + mode).classList.add('active', 'border-primary', 'bg-red-50/20');
            
            if (mode === 'cod') {
                document.getElementById('cod-row-balance').classList.remove('hidden');
                document.getElementById('cod-row-fee').classList.remove('hidden');
            } else {
                document.getElementById('cod-row-balance').classList.add('hidden');
                document.getElementById('cod-row-fee').classList.add('hidden');
            }
            calculateTotal();
        }

        function calculateTotal() {
            const qty = parseInt(document.getElementById('bidQty').value) || 0;
            const subtotal = qty * currentPrice;
            const advance = subtotal * 0.1;
            const balance = subtotal * 0.9;
            const fee = paymentMode === 'cod' ? subtotal * 0.1 : 0;
            const total = paymentMode === 'cod' ? (subtotal + fee) : subtotal;

            // From Image: 
            // Current Price x 5 = 1,61,66,197
            // Advance = 16,16,619
            // Balance = 1,45,49,577
            // Fee = 16,16,619
            // Total = 1,61,66,197 (Wait, subtotal is total? Then fee is included in subtotal or added? Actually total = subtotal usually)

            document.getElementById('display-qty').textContent = qty;
            document.getElementById('display-subtotal').textContent = `₹${subtotal.toLocaleString()}`;
            document.getElementById('display-advance').textContent = `₹${advance.toLocaleString()}`;
            document.getElementById('display-balance').textContent = `₹${balance.toLocaleString()}`;
            document.getElementById('display-fee').textContent = `+₹${fee.toLocaleString()}`;
            const totalDisplay = document.getElementById('billing-total-payable');
            if (totalDisplay) {
                totalDisplay.textContent = `₹${total.toLocaleString()}`;
            }

            // Update chips
            document.querySelectorAll('.qty-chip').forEach(chip => {
                if (parseInt(chip.dataset.qty) === qty) {
                    chip.classList.add('bg-primary', 'text-white', 'shadow-lg');
                    chip.classList.remove('bg-gray-50', 'text-gray-400');
                } else {
                    chip.classList.remove('bg-primary', 'text-white', 'shadow-lg');
                    chip.classList.add('bg-gray-50', 'text-gray-400');
                }
            });
        }

        document.addEventListener('DOMContentLoaded', calculateTotal);

        // Auto Close Toast
        setTimeout(() => {
            const toast = document.getElementById('bid-toast');
            if (toast) {
                toast.style.opacity = '0';
                toast.style.transform = 'translateY(20px)';
                toast.style.transition = 'all 0.5s';
            }
        }, 8000);
    </script>

    <style>
        .no-scrollbar::-webkit-scrollbar { display: none; }
        @keyframes slide-up {
            from { transform: translateY(100%); opacity: 0; }
            to { transform: translateY(0); opacity: 1; }
        }
        .animate-slide-up { animation: slide-up 0.6s cubic-bezier(0.2, 0.8, 0.2, 1); }
    </style>

</body>
</html>
